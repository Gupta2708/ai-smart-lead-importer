{
  "name": "@groweasy/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc --noEmit -p tsconfig.json", "test": "vitest run" },
  "dependencies": { "date-fns": "^4.1.0", "zod": "^3.24.2" },
  "devDependencies": { "typescript": "^5.7.3", "vitest": "^3.0.7" }
}
